@model IEnumerable<Formula1.Models.Posicao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomePiloto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomeEquipe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PosicaoGrid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PontosCorrida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomePiloto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeEquipe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PosicaoGrid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PontosCorrida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
